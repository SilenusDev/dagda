# Dagda-Lite - Configuration Environment Variables
# PRODUCTION Configuration (serveur avec IP)

# Project Identification
PROJECT_NAME=dagda-lite
HOST=192.168.1.43
DEFAULT_HOST=192.168.1.43
CONTAINER_REGISTRY=192.168.1.43:8908

# Service Ports (890X range)
DB_PORT=8901
API_PORT=8902
ADMIN_PORT=8903
WORKFLOW_PORT=8904
LLAMA_PORT=8905
LLAMA_CONTAINER_PORT=8000  # Port interne du conteneur llama.cpp ne peut etre changé
QWEN_PORT=8906
YARN_PORT=8907
VITE_PORT=8900

# FastAPI Configuration
FASTAPI_ENV=production
FASTAPI_WORKERS=4
APP_HOME=/home/app

# Database Configuration (MariaDB)
DB_ROOT_PASSWORD=secure_root_password
DB_NAME=application_data
DB_USER=app_user
DB_PASSWORD=secure_app_password

# DAGDA Orchestrator Database Configuration
DAGDA_DB_NAME=dagda_db
DAGDA_DB_USER=dagda_db_user
DAGDA_DB_PASSWORD=secure_dagda_db_password
DAGDA_DB_HOST=${HOST}
DAGDA_DB_PORT=${DB_PORT}

# System Paths
APP_DIR=/var/www/dagda-lite
DATA_DIR=${DAGDA_ROOT}/data
LOGS_DIR=/var/log/dagda-lite

# Application Configuration
AUTO_SHUTDOWN_TIMEOUT=300
RESOURCE_CHECK_INTERVAL=30
MAX_SERVICES_SIMULTANEOUS=10

# === DAGDA-LITE ENGINE PATHS ===
# Project root (MUST be configured for your installation)
DAGDA_ROOT=/home/sam/Bureau/dev/production/dagda-lite

# Engine scripts (chemins réels vérifiés)
OLLAMH_SCRIPT=${DAGDA_ROOT}/dagda/awens-utils/ollamh.sh
TEINE_ENGINE_SCRIPT=${DAGDA_ROOT}/dagda/awen-core/teine_engine.sh
IMBAS_LOGGING_SCRIPT=${DAGDA_ROOT}/dagda/awens-utils/imbas-logging.sh
TARANIS_SCRIPT=${DAGDA_ROOT}/dagda/eveil/taranis.sh
LUG_SCRIPT=${DAGDA_ROOT}/dagda/eveil/stop_all.sh

# === DAGDA-LITE DIRECTORY STRUCTURE ===
# Service directories (nouvelle architecture)
CAULDRON_DIR=${DAGDA_ROOT}/cauldron
CROMLECH_DIR=${CAULDRON_DIR}/cromlech
MUIRDRIS_DIR=${CAULDRON_DIR}/muirdris
FIANNA_DIR=${CAULDRON_DIR}/fianna
OGMIOS_DIR=${CAULDRON_DIR}/ogmios
# GEASA_DIR supprimé - Yarn déplacé dans cromlech
SIDHE_DIR=${DAGDA_ROOT}/sidhe

# Engine directories
DAGDA_DIR=${DAGDA_ROOT}/dagda
EVEIL_DIR=${DAGDA_DIR}/eveil
AWENS_UTILS_DIR=${DAGDA_DIR}/awens-utils
AWEN_CORE_DIR=${DAGDA_DIR}/awen-core

# Data directories
DOLMEN_DIR=${DAGDA_ROOT}/dolmen

# Environment indicator
DAGDA_ENV=production
